# 火山引擎云服务器部署指南

## 一、服务器选购建议

| 配置项 | 推荐值 | 说明 |
|--------|--------|------|
| 规格 | ecs.g3i.large (2核4G) 或 ecs.g3i.medium (2核2G) | 10个小朋友足够用 |
| 系统 | Ubuntu 22.04 LTS | 长期支持版，软件源丰富 |
| 系统盘 | 40GB SSD | 代码+图片+数据库不到20MB，余量充足 |
| 带宽 | 按量计费 5Mbps峰值 | 一天最多1小时，按量更省钱 |
| 安全组 | 放行 22(SSH)、80(Nginx)、443(HTTPS)、3000(直连) | |

> **计费方式**：10个小朋友每月用约30小时，按量计费约 ¥15-20/月；如果24小时不间断运行则包月更划算。

## 二、环境准备

### 1. SSH 连接服务器

```bash
ssh root@你的公网IP
```

### 2. 安装 Node.js 22

```bash
# 安装 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
source ~/.bashrc

# 安装 Node.js 22
nvm install 22
nvm use 22
node -v  # 确认输出 v22.x.x
```

> 如果 raw.githubusercontent.com 被墙，用镜像：
> ```bash
> curl -o- https://gitee.com/mirrors/nvm/raw/master/install.sh | bash
> ```

### 3. 安装 Git

```bash
apt update && apt install -y git
```

## 三、部署项目

### 1. 克隆代码

```bash
cd /opt
git clone https://gitee.com/你的用户名/study-platform.git
cd study-platform
```

### 2. 启动服务

```bash
# 直接启动（测试用）
node --experimental-sqlite server/server.js

# 看到 "Server running on port 3000" 即成功
# Ctrl+C 先停掉，下面用 systemd 做持久化
```

### 3. 配置 systemd 服务（开机自启 + 崩溃重启）

```bash
cat > /etc/systemd/system/study-platform.service << 'EOF'
[Unit]
Description=Study Platform Server
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/study-platform
ExecStart=/root/.nvm/versions/node/v22.*/bin/node --experimental-sqlite server/server.js
Environment=PORT=3000
Environment=NODE_ENV=production
Restart=always
RestartSec=5
StandardOutput=append:/opt/study-platform/log.txt
StandardError=append:/opt/study-platform/log.txt

[Install]
WantedBy=multi-user.target
EOF

# 重新加载 systemd
systemctl daemon-reload
systemctl enable study-platform
systemctl start study-platform

# 查看状态
systemctl status study-platform
```

> **注意**：`ExecStart` 中的 node 路径需要替换为实际路径。用 `which node` 查看。
> 例如：`ExecStart=/root/.nvm/versions/node/v22.11.0/bin/node --experimental-sqlite server/server.js`

### 4. 验证访问

```bash
# 本机测试
curl http://localhost:3000

# 浏览器访问
http://你的公网IP:3000
```

## 四、安全组配置

在火山引擎控制台 → 云服务器 → 安全组：

| 规则 | 端口 | 来源 | 说明 |
|------|------|------|------|
| SSH | 22 | 你的IP/32 | 仅限你的IP |
| HTTP | 80 | 0.0.0.0/0 | Nginx 用 |
| HTTPS | 443 | 0.0.0.0/0 | HTTPS 用 |
| 直连 | 3000 | 0.0.0.0/0 | 临时访问（配好Nginx后可关） |

## 五、（可选）Nginx 反向代理 + HTTPS

### 1. 安装 Nginx

```bash
apt install -y nginx
```

### 2. 配置反向代理

```bash
cat > /etc/nginx/sites-available/study-platform << 'EOF'
server {
    listen 80;
    server_name your-domain.com;  # 换成你的域名或IP

    client_max_body_size 10M;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cache_bypass $http_upgrade;
    }
}
EOF

ln -sf /etc/nginx/sites-available/study-platform /etc/nginx/sites-enabled/
nginx -t && systemctl reload nginx
```

### 3. 配置 HTTPS（需要域名）

```bash
apt install -y certbot python3-certbot-nginx
certbot --nginx -d your-domain.com
```

## 六、环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `PORT` | 3000 | 服务端口 |
| `DB_PATH` | server/data.db | 数据库文件路径 |
| `BACKUP_DIR` | server/backups | 数据库备份目录 |
| `NODE_ENV` | - | 设为 production |

在 systemd 中修改环境变量后需：
```bash
systemctl daemon-reload
systemctl restart study-platform
```

## 七、数据备份

- 数据库自动每天凌晨 1:00 备份到 `server/backups/`，保留最近 14 份
- 手动备份：`cp server/data.db server/data-backup-$(date +%Y%m%d).db`
- 建议定期把 `server/data.db` 下载到本地保存

## 八、日常运维

```bash
# 查看服务状态
systemctl status study-platform

# 重启服务
systemctl restart study-platform

# 查看实时日志
tail -f /opt/study-platform/log.txt

# 更新代码
cd /opt/study-platform
git pull
systemctl restart study-platform
```

## 九、从旧服务器迁移数据

如果之前有数据，把 `data.db` 上传到新服务器：

```bash
# 本地上传到服务器
scp server/data.db root@你的IP:/opt/study-platform/server/data.db

# 重启服务生效
systemctl restart study-platform
```
